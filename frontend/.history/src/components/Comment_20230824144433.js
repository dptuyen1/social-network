const Comment = (props) => {
    const user = props.user;
    const comment = props.comment;
    return (
        <>
            <div className="d-flex"></div>
        </>
    );
};

export default Comment;
